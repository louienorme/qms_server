# qms_server

### How to run

#### Dev Environment 
1. Run `yarn install` or `npm install`
2. Run `yarn start:dev` 

### Production Environment
1. Run `yarn install` or `npm install`
2. Run `yarn build`
3. Run `yarn start:prod` 